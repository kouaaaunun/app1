<body>
<% if flash[:notice] %>
<p id="notice">
  <%= flash[:notice] %></p>
<% end %>

<h1>Books</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Body</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
      <% @books.each do |book| %>

      <tr>
        <td><%= book.title %></td>
        <td><%= book.body %></td>
        <td><a class="show"><%= link_to 'Show', book_path(book) %></a></td>
        <td><a class="edit"><%= link_to 'Edit', edit_book_path(book) %></a></td>
        <td><a class="destroy"><%= link_to "Destroy", destroy_book_path(book), method: :delete, "data-confirm" => "Are you sure？" %></a></td>
      </tr>
      <% end %>

  </tbody>
</table>


<h2>New book</h2>
<%= form_for(@book) do |f| %>

  <div class="field">
    <label for="book_title">Title</label>
      <%= f.text_field :title %>
  </div>

  <div class="field">
    <label for="book_body">Body</label>
      <%= f.text_area :body %><br>
  </div>

  <div class="actions">
      <%= f.submit 'Create Book' %>
  </div>

<% end %>

</body>